aFrame
======

a jQuery plugin to handle ajax iFrame like sections

```jQuery
$('.aframe').aFrame({
  callbacks: {
    onLoad: function($frame, setup, data){
      //callback on load.
    }
  }
});
```